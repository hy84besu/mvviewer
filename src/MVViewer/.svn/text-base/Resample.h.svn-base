#ifndef RESAMPLE_H
#define RESAMPLE_H

#include "CImg_mod.h"
using namespace cimg_library;

const float factor = 0.5f; // Determine l'ecart type de la Gaussienne utilisee pour le sous echantillonage

void Resample(const CImg<> &in, CImg<> &out);
CImg<> Resample(const CImg<> &in, int width, int height, int depth = 1);


#endif
